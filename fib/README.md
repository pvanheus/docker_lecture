If you want to use this locally build the docker image with:

    docker build -t fib .

and run with:

    docker run --rm fib

Or you can use the version from quay.io, look for it under `pvanheus`.

